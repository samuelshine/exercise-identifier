fastapi>=0.115.0
uvicorn[standard]>=0.30.6

sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.31.0
pydantic>=2.12.0
pydantic-settings>=2.5.2

# Schema migrations
alembic>=1.13.2

# Per-IP rate limiting for the public search endpoint
slowapi>=0.1.9

# Structured (JSON) logging for production observability
python-json-logger>=2.0.7

ollama>=0.3.3
chromadb>=1.0.0
tqdm>=4.66.5

# Retry logic for flaky Ollama calls
tenacity>=9.0.0

# Async file I/O (used by embed pipeline & video handler)
aiofiles>=24.1.0

# Required for FastAPI File/Form uploads (Phase 2 video endpoint)
python-multipart>=0.0.12

# AWS S3 ephemeral video storage (Phase 2)
# boto3>=1.35.0

# Video processing (Phase 2)
# mediapipe>=0.10.0
